<?xml version="1.0" encoding="UTF-8"?>
<project>
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ch.sbb.polarion.extensions</groupId>
        <artifactId>ch.sbb.polarion.extension.generic</artifactId>
        <version>15.10.1</version>
    </parent>

    <artifactId>ch.sbb.polarion.extension.extension-name</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>open-source-polarion-java-repo-template ... extension of Polarion ALM</name>
    <description>This is a Polarion extension which provides ...</description>
    <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.extension-name</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>SBB Polarion Team</name>
            <email>polarion-opensource@sbb.ch</email>
            <organization>SBB AG</organization>
            <organizationUrl>https://www.sbb.ch</organizationUrl>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.extension-name.git</connection>
        <developerConnection>scm:git:ssh://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.extension-name.git</developerConnection>
        <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.extension-name/tree/main</url>
    </scm>

    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/SchweizerischeBundesbahnen/ch.sbb.polarion.extension.extension-name/issues</url>
    </issueManagement>

    <properties>
        <maven-jar-plugin.Extension-Context>extension-name</maven-jar-plugin.Extension-Context>
        <maven-jar-plugin.Automatic-Module-Name>ch.sbb.polarion.extension.extension_name</maven-jar-plugin.Automatic-Module-Name>

        <web.app.name>${maven-jar-plugin.Extension-Context}</web.app.name>

        <!-- README.md -> about.html conversion. The parent defaults to failOnError=false so a local
             build without a GITHUB_TOKEN stays lenient; CI sets the environment variable to true, and a
             broken conversion then fails the build instead of shipping an empty About page. Unset, the
             property does not resolve to a boolean and the plugin reads it as false. -->
        <!--suppress UnresolvedMavenProperty -->
        <markdown2html-maven-plugin.failOnError>${env.MARKDOWN2HTML_MAVEN_PLUGIN_FAIL_ON_ERROR}</markdown2html-maven-plugin.failOnError>
    </properties>

    <dependencies>
        <dependency>
            <groupId>ch.sbb.polarion.extensions</groupId>
            <artifactId>ch.sbb.polarion.extension.generic.app</artifactId>
            <version>${project.parent.version}</version>
        </dependency>

        <!-- generic's test helpers: CurrentContextExtension / CurrentContextConfig (the JUnit extension
             that gives a test a Polarion context) and the REST test bases. Every extension with real
             tests depends on it. Note when building generic from source: `-DskipTests` still packages
             this test-jar, `-Dmaven.test.skip=true` does not, and the consumer build then fails
             resolving it. -->
        <dependency>
            <groupId>ch.sbb.polarion.extensions</groupId>
            <artifactId>ch.sbb.polarion.extension.generic.app</artifactId>
            <version>${project.parent.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-clean-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>ch.sbb.maven.plugins</groupId>
                <artifactId>markdown2html-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
            </plugin>

            <!-- Re-formats the docs/openapi.json that swagger-maven-plugin-jakarta writes, by running the
                 same pre-commit hooks that guard it, so the generated file matches the committed one and
                 a build never leaves a formatting-only diff behind. Version and execution come from the
                 generic parent. -->
            <plugin>
                <groupId>io.github.grigoriev</groupId>
                <artifactId>pre-commit-run-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>io.swagger.core.v3</groupId>
                <artifactId>swagger-maven-plugin-jakarta</artifactId>
                <configuration>
                    <outputFormat>JSON</outputFormat>
                    <!-- Stable ordering, so a rebuild does not reshuffle docs/openapi.json into a diff
                         that says nothing. -->
                    <sortOutput>true</sortOutput>
                    <resourcePackages>
                        <!-- `info` backs the About page (/version, /configuration-properties,
                             /configuration-status, /readme, /user-guide). Uncomment `roles` when the
                             extension adds RSP's Authorization page, and `settings` when it gets its
                             first named setting. -->
                        <package>ch.sbb.polarion.extension.generic.rest.controller.info</package>
<!--                        <package>ch.sbb.polarion.extension.generic.rest.controller.roles</package>-->
<!--                        <package>ch.sbb.polarion.extension.generic.rest.controller.settings</package>-->
                        <package>ch.sbb.polarion.extension.generic.rest.model</package>
                        <package>ch.sbb.polarion.extension.extension_name.rest.controller</package>
<!--                        <package>ch.sbb.polarion.extension.extension_name.rest.model</package>-->
                    </resourcePackages>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
